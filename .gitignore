# 변환 임시물 / 결과
_glycan_work/
*.mzML
*_glycans.xlsx

# Thermo 변환기 번들(용량 큼) — setup_thermo.py 로 각 PC에서 받음
glyco/vendor/

# 파이썬
__pycache__/
*.pyc
.venv/
build/
dist/
*.spec

# 테스트용 대용량 mzML(미커밋)
tests/data/sample.mzML

# pancreatin 평가용 대용량 mzML(미커밋)
tests/data/pancreatin.mzML

# 데이터/결과 폴더 (대용량 .raw 등 — 공개 금지)
data/
result/

# 실행 결과물 폴더
outputs/
